[pytest]
pythonpath = src
testpaths = tests
addopts = -ra --strict-markers --strict-config
markers =
    unit: Fast, isolated unit tests.
    integration: Cross-module tests that may involve I/O boundaries.
    ui: Browser/UI tests (Playwright), opt-in via MIMIR_RUN_UI_TESTS=1.
